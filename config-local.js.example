// config-local.js.example
// This is a template file. Copy this to config-local.js and add your real API keys.
// config-local.js is git-ignored and will not be committed to the repository.

// HOW TO USE:
// 1. Copy this file: cp config-local.js.example config-local.js
// 2. Edit config-local.js and replace the placeholder values with your real API keys
// 3. NEVER commit config-local.js to the repository

const CONFIG_LOCAL = {
    // Google Maps API Configuration
    googleMaps: {
        apiKey: 'YOUR_GOOGLE_MAPS_API_KEY_HERE'  // Replace with your actual Google Maps API key
    },
    
    // Google Sheets API Configuration (Optional - for Admin Dashboard)
    // By default, the Google Maps API key is used for Google Sheets API too
    // Only specify this if you want to use a different API key for Google Sheets
    googleSheets: {
        apiKey: ''  // Leave empty to use Google Maps API key, or provide a different key
    },
    
    // EmailJS Configuration (REQUIRED for admin notifications to work)
    // See EMAILJS_SETUP_INSTRUCTIONS.md for detailed setup guide
    emailjs: {
        enabled: true,  // Set to true to enable email features
        publicKey: 'YOUR_EMAILJS_PUBLIC_KEY_HERE',      // From EmailJS dashboard → Account → General
        serviceId: 'YOUR_EMAILJS_SERVICE_ID_HERE',      // From EmailJS dashboard → Email Services
        templateId: '',                                  // Leave empty if not sending customer confirmations
        
        // Admin Notification Configuration
        // When enabled, sends email to admin whenever a quote is submitted
        adminNotification: {
            enabled: true,  // Already enabled in config.js
            adminEmail: 'huabaohuang622@gmail.com',  // Email where notifications are sent
            adminTemplateId: 'YOUR_ADMIN_TEMPLATE_ID_HERE'  // From EmailJS dashboard → Email Templates
        }
    }
    
    // Note: You only need to include the values you want to override from config.js
    // Any values not specified here will use the defaults from config.js
};

// Don't change this line - it's needed for the config to work
if (typeof module !== 'undefined' && module.exports) {
    module.exports = CONFIG_LOCAL;
}
